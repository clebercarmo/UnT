version: '3'

services:
  postgres:
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: DfzKF@U!5zGBA!
      POSTGRES_PASSWORD: "w@m3q@9JWEMY&QcW"
      POSTGRES_DB: ufeiradb
    ports:
      - "15432:5432"
    networks:
      - postgres-network      

networks: 
  postgres-network:
    driver: bridge